
version: 2

models:
 - name: sources
   ...

metrics:
  - name: users
    label: count usersid
    model: ref('sources')

    type: count_distinct
    sql: user_id 

    timestamp: time
    time_grains: [day, week, month, quarter, year]

    dimensions:
      - event
      - browser
      - os
      - new
    
    filters:
